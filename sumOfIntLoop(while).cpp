#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <time.h>

int main(){
	srand(time(0));
	int a;
	a>=1;
	int b;
	b=rand();
	while(a>=1){
		int sum=a+b;
		printf("sum of both integers : %d",sum);
	}
return 0;
}
	
